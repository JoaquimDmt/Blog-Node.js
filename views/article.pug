extends layout.pug
 
block variables
    - let title = 'Article';
 
block content
    h1= article.title
    h2= "Article par "+article.author.name 
    form(action="contact/send", method="post")
        input(type="text", name="text")
        textarea(name="message", cols="30", rows="10")
        button(type="submit") Send